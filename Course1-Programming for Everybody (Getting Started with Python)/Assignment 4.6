def computepay(h,r):
    if h <= 40:
        return(h*r)
    elif h > 40:
        return(40 * r + (h-40)*1.5*r)
    else:
    	return 42.37

hrs = input("Enter Hours:")
h = float(hrs)
r = float(input("Enter rate per hour"))
p = computepay(h,r)
print("Pay",p)